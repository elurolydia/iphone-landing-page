import React from 'react'

const Buttons = () => {
    return (
        <div>
            <button className="solid_btn">Order now</button>
            <button className="outline_btn">View specs</button>
        </div>
    )
}

export default Buttons
